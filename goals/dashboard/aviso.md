
**Nota sobre el dashboard web**: Para funcionar localmente, serví la carpeta `goals/` con un servidor HTTP simple (ej. `python3 -m http.server 8080` desde la raíz del repo) y accedé a `http://localhost:8080/goals/dashboard/`. El archivo `registry.db` debe ser accesible desde el navegador.
